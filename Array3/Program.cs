using System;

namespace Array3
{
    class Program
    {
        static void Main(string[] args)
        {
            /* 
           Console.WriteLine("Digite as cores SEPARADAS POR | :");
            string[] cores = Console.ReadLine().Split('|');

            for(int i=0;i<cores.Length;i++)
                Console.WriteLine("Você digitou {0}",cores[i]);
 */
/* 
            Console.WriteLine("Digite os numeros SEPARADAS POR ; :");
            string[] n = Console.ReadLine().Split(';');
            double r=0;
            
            for(int i=0;i<n.Length;i++)
                r+=Convert.ToDouble(n[i]);

            Console.WriteLine("soma: {0}\nmédia: {1}",r,r/n.Length); */


            
        /*     Console.WriteLine("Digite os numeros SEPARADAS POR ; :");
            string[] n = Console.ReadLine().Split(';');
            double r=0;
            
            foreach(var i in n)
                r+= Convert.ToDouble(i);

            Console.WriteLine("soma: {0}\nmédia: {1}",r,r/n.Length);
 */
        }
    }
}
